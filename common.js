// common behavior for all pages
// currently only the dark mode toggle, but you can add more shared functions here

function toggleDarkMode() {
    document.body.classList.toggle('dark-mode');
}
